using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu]
public class AcidSpill : BaseAttack
{
    public override void spillAttack(GameObject attackItem, Vector3 originPos)
    {
        base.spillAttack(attackItem, originPos);
    }
}
